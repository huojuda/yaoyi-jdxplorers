# EdgeOne Pages 部署核对清单（药忆 v1.4.0）

> 用途：按顺序逐项勾选，全部完成即可上线。静态文件走控制台手动上传，无需 CLI。
> 生成时间：2026-09-20
> **复核时间：2026-09-22**（由本地代码 + 线上实测逐项核对，标注见每项）

## 复核结论速览

| 阶段 | 状态 | 说明 |
|---|---|---|
| 阶段 0 代码硬门槛 | ✅ 4/4 | 短信已接入（fail-closed）、v1.4.0 已提交 `748d93a` |
| 阶段 1 静态文件 | ❌ **未上线** | 线上仍是 v1.3.0（9/11 15:13），sw.js 仍是 v2.1.0 |
| 阶段 2 边缘函数 | ❓ 需控制台核对 | 6 个文件本地齐备，ask.js + auth.js 本次有改动必须重传 |
| 阶段 3 环境变量 | ❓ 需控制台核对 | 本地无法验证；新增 8 个 SMS_* 变量 |
| 阶段 4 短信网关 | ⚠️ 代码就绪 | 双服务商实现已完成并交叉验证，**等签名/模板审核通过 + 填环境变量** |
| 阶段 5 域名与 HTTPS | ✅ **已完成** | 证书 CN=yaoyi.site，有效期至 2026-12-09 |
| 阶段 6 真机自测 | ❌ 阻塞 | 依赖阶段 1 先上线 |

> **2026-09-22 第二轮进展**：v1.4.0 已提交 git；短信网关（腾讯云 TC3 / 阿里云 POP）已在
> `auth.js` 与 `server.py` 双端实现并用固定时间戳做过签名一致性验证；
> 创作说明定稿 `创作说明_药忆_500字_定稿.docx`（整篇 483 字，含标题）。

---

## 阶段 0 · 上线前代码硬门槛

- [x] **重置验证码接入真实短信**（2026-09-22 完成）
  - ✅ `auth.js` / `server.py` / `index.html` 三处 `demoCode` 回显已改为「短信下发 + fail-closed」
  - ✅ 支持腾讯云 SMS（TC3-HMAC-SHA256）与阿里云短信（POP HMAC-SHA1），`SMS_PROVIDER` 切换
  - ✅ 未配置短信 → 返回 503；显式 `SMS_DEMO_FALLBACK=true` 才回显（评审联调用，上线须移除）
  - ✅ 发送失败自动撤销冷却与计数，避免用户空等 60 秒
- [x] 确认 `api_config.json`（本地，含 Key）不会被上传到仓库/托管
  - ✅ 复核：未入库（`git ls-files` 无此文件），`.gitignore` 已屏蔽
- [x] 已在本地 `node --check` 与 `python -m py_compile` 通过全部改动（已完成）
- [x] **本次 v1.4.0 改动已提交 git 或备份（便于回滚）**
  - ✅ 复核：2026-09-22 已提交 `748d93a`，`git status` 干净
  - ⚠️ 短信相关改动（`auth.js` `server.py` `index.html`）在此提交之后，需再提交一次

## 阶段 1 · 静态文件上传

> ❌ **复核：全部未更新。** 线上三个文件 Last-Modified 均为 `2026-09-11 15:13 GMT`，
> 线上 `sw.js` 的 `CACHE_NAME` 仍是 `yaoyi-v2.1.0`（本地已为 `yaoyi-v3.0.0`），
> 线上 index.html 236,013 B vs 本地 245,675 B → **线上跑的是 v1.3.0，v1.4.0 从未上线**

- [ ] 上传 `index.html`（根目录）
- [ ] 上传 `manifest.json`（根目录）
- [ ] 上传 `sw.js`（根目录，版本 yaoyi-v3.0.0）
- [x] 确认三文件路径为相对路径 `./`，scope 为 `./`（本地已符合）

## 阶段 2 · 边缘函数路由

> ❓ 是否上传需控制台确认。**重点：`ask.js` 本次新增 195 行（联合用药大模型深度分析），必须重传**

| 路由 | 文件 | 已上传 |
|---|---|---|
| `/api/auth` | edge-functions/api/auth.js | [ ] |
| `/api/recognize` | edge-functions/api/recognize.js | [ ] |
| `/api/ask` | edge-functions/api/ask.js | [ ] |
| `/api/relation` | edge-functions/api/relation.js | [ ] |
| `/api/events` | edge-functions/api/events.js | [ ] |
| `/api/pair` | edge-functions/api/pair.js | [ ] |

## 阶段 3 · 环境变量注入

> ❓ 需控制台确认。注意 `server.py` 已从 api_config.json 改为读环境变量，本地/线上取值方式需对齐

| 变量名 | 值来源 | 已配置 |
|---|---|---|
| `UPSTASH_REDIS_REST_URL` | Upstash 控制台 | [ ] |
| `UPSTASH_REDIS_REST_TOKEN` | Upstash 控制台 | [ ] |
| `YAOWI_API_KEY` | 火山方舟 | [ ] |
| `YAOWI_API_ENDPOINT` | 可选，默认北京区 | [ ] |
| `YAOWI_MODEL_ID` | 可选，默认 doubao-seed-2-0-mini-260428 | [ ] |
| `SMS_PROVIDER` | `tencent` 或 `aliyun` | [ ] |
| `SMS_SECRET_ID` | 腾讯云 SecretId / 阿里云 AccessKeyId | [ ] |
| `SMS_SECRET_KEY` | 腾讯云 SecretKey / 阿里云 AccessKeySecret | [ ] |
| `SMS_SIGN_NAME` | 已审核的短信签名（不带方括号） | [ ] |
| `SMS_TEMPLATE_ID` | 腾讯云 TemplateId / 阿里云 TemplateCode | [ ] |
| `SMS_SDK_APP_ID` | 仅腾讯云：SdkAppId | [ ] |
| `SMS_REGION` | 可选，默认 ap-guangzhou / cn-hangzhou | [ ] |

> 短信模板变量：腾讯云 `[验证码, 有效分钟数]`；阿里云 `{"code":…, "minutes":…}`。两边模板要按服务商要求分别建。

> ⚠️ 环境变量属敏感项：确认不写入前端代码、不写入公开日志。

## 阶段 4 · 短信网关接入（上线必须）

> ⚠️ **代码已就绪（2026-09-22），剩下的是账号侧手续 + 填环境变量。**
> 前置条件：① 实名认证的腾讯云/阿里云账号 ② 短信签名审核通过（个人主体通常需 1–3 工作日）
> ③ 模板审核通过 ④ 短信套餐有余额（腾讯云个人可领 100 条免费额度）
> 审核未通过期间，可临时设 `SMS_DEMO_FALLBACK=true` 保证重置流程可用，审核通过后删除该变量。

- [x] 选定短信服务商（腾讯云 / 阿里云双实现已就绪）——**待你确认用哪一家**
- [x] 在 `auth.js` 的 `sendcode` 中调用真实短信 API（已移除响应 `demoCode`）
- [x] 新增环境变量（`SMS_PROVIDER` / `SMS_SECRET_ID` / `SMS_SECRET_KEY` / `SMS_SIGN_NAME` / `SMS_TEMPLATE_ID` / `SMS_SDK_APP_ID` 等）
- [x] 本地 `server.py` 同步镜像，`demoCode` 一并移除
- [x] 保留现有限制：5 分钟有效、60 秒冷却、每小时 5 次、错 5 次作废
- [ ] **待做**：控制台开通签名与模板 → 填入环境变量 → 真机收码验证

## 阶段 5 · 域名与 HTTPS

- [x] 绑定自有域名 yaoyi.site（**2026-09-22 实测通过**）
- [x] CNAME / 解析生效（`curl -I` 返回 200，Server: edgeone makers）
- [x] HTTPS 证书生效（**2026-09-22 实测**：CN=yaoyi.site，TrustAsia DV TLS RSA CA 2025，
      有效期 2026-09-11 → 2026-12-09，无 COMMON_NAME_INVALID）

## 阶段 6 · 上线后自测（真机）

- [ ] 拍照识药：后置摄像头 + 相册两通道
- [ ] 四问结果页 + 0.8 倍慢速语音
- [ ] 防重复：同药二次识别 → 红色拦截 + AI 话术
- [ ] 联合用药：≥2 种药 → 规则速查 + 大模型深度分析
- [ ] 账号：注册 / 登录 / 发验证码 / 重置 / 注销
- [ ] 家属绑定：患者 8 位码 → 家属绑定 → 实时收到服药/求助事件
- [ ] PWA：添加主屏幕；飞行模式刷新仍可打开外壳
- [ ] 新版本发布后 SW 自动更新（在线刷新即最新）

---

## 参赛优先级（截止 2026-10-07，剩 15 天）

> 部署清单之外，还有三项参赛交付物未完成，与部署并列推进

| 编号 | 事项 | 状态 | 证据 |
|---|---|---|---|
| A | 创作说明 ≤500 字定稿 | ✅ 已完成 | `创作说明_药忆_500字_定稿.docx` 整篇 **483 字**（含标题副标题），最严格口径也不超限 |
| B | 演示视频 MP4 ≤60s | ❌ 无 | 仅有 `演示视频分镜脚本.md`，无成片。**这是当前最大的未完成项** |
| C | 用户访谈纪要 | ❌ 无 | 改进计划 P1-2，未产出 `用户访谈纪要.md` |
| D | 团队信息（学校/姓名/指导教师） | ❓ 待你提供 | 改进计划「待确认事项 6」从未回答；创作说明当前无团队段落，若需写入得再压缩约 45 字 |

---

## 上线后需要你关注的事
- 线上数据与本地隔离，测试请用真实手机号
- 任何敏感操作（重置、注销）上线后再走一遍真机确认
- 医疗免责声明已在应用内固定展示，勿删
- ⚠️ 线上 sw.js 版本落后本地两个大版本，上传后用户首次刷新会触发缓存重建，需复测 PWA
